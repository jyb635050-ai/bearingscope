# BearingScope 设计系统

## 1. 设计方向

视觉关键词：**精密、克制、坚固、可追溯**。

界面从轴承本体提取三类语言：石墨与枪灰代表钢制滚道，拉丝铜/黄铜代表保持架，电青色代表测量仪器与交互反馈。装饰必须服务于层级和品牌识别，不能把界面做成游戏面板或霓虹赛博风。

默认深色主题，信息密度接近专业终端；浅色主题保持相同层级和工业质感。所有主题均以 WCAG AA 对比度为底线。

## 2. 视觉 Token

以下命名用于约束实现，实际 CSS 变量应保持语义一致：

| Token | 深色主题 | 用途 |
| --- | --- | --- |
| `--bg` | `#080D12` | 页面背景 |
| `--surface-1` | `#0E151D` | 侧栏、顶部栏 |
| `--surface-2` | `#131C26` | 卡片与面板 |
| `--surface-3` | `#192532` | 悬停、输入和嵌套区域 |
| `--border` | `#283746` | 默认边框 |
| `--text` | `#EEF4F7` | 主要文本 |
| `--text-muted` | `#8FA1AF` | 辅助文本 |
| `--cyan` | `#28C7D9` | 选中、链接、焦点与数据强调 |
| `--cyan-soft` | `rgba(40,199,217,.12)` | 选中背景 |
| `--brass` | `#C69A55` | 高价值信息与轴承保持架元素 |
| `--copper` | `#A96F45` | 次要金属强调 |
| `--danger` | `#E66B68` | 风险与错误 |
| `--success` | `#62C29A` | 成功与可用状态 |

浅色主题使用暖灰白背景、深石墨正文、降低饱和度的青色与铜色。状态不能只靠颜色表达：同时使用文字、图标或形状。

### 字体

- 中文 UI：`Inter`, `Noto Sans SC`, `Microsoft YaHei`, sans-serif。
- 英文与数字：`Inter`, `Segoe UI`, sans-serif。
- 品牌字标或大标题可使用窄体工业无衬线，但正文不使用装饰字体。
- 默认正文 `14–16px / 1.6`；辅助信息不低于 `12px`；关键数字使用等宽数字特性。

### 尺寸与形态

- 基础间距：4px；常用间距为 8、12、16、24、32px。
- 卡片圆角 12–16px，按钮与标签圆角 8–999px。
- 边框以 1px 冷灰线为主；阴影轻微且只用于区分层级。
- 动画时长 140–220ms；只动画 `opacity` 与 `transform`。启用 `prefers-reduced-motion` 时移除非必要位移。

## 3. 页面框架

### 桌面 ≥ 1200px

- 固定左侧栏宽约 232px，顶部显示带轴承图形的 BearingScope / 轴承视界字标。
- 主内容区使用 12 列网格，最大阅读宽度约 1480px；精选页可采用主信息流 + 右侧品牌雷达。
- 顶部工具栏固定或粘性，包含移动菜单占位、页面标题、搜索、语言和主题切换。
- 时间线与热点排行使用紧凑垂直节奏；详情从右侧滑出，不打断原页面状态。

### 平板 768–1199px

- 侧栏收窄或折叠为抽屉；页面以 8 列网格组织。
- 品牌雷达移至主内容下方；筛选器允许横向滚动，关键筛选始终可见。

### 移动 < 768px

- 使用顶部栏和抽屉导航，内容单列排列。
- 搜索和筛选分两行或打开筛选面板；筛选胶囊可横向滚动但不截断标签。
- 详情面板占满视口，保留明确关闭按钮和浏览器返回行为。
- 交互目标至少 44×44px；长中英文标题换行，不使用水平页面滚动。

## 4. 核心组件

### 导航

六个主导航项使用图标 + 双语可翻译标签。当前项使用青色左侧标记、描边和浅青背景；不能只依靠字体颜色。

### 信息卡与时间线

- 层级顺序：来源/时间 → 标题 → 摘要 → 标签/关键事实 → 操作。
- 标题最多在列表中显示三行；完整内容可在详情面板读取。
- 来源等级、`DEMO`、微信、开放获取等徽章必须有文字标签。
- 收藏按钮具备默认、悬停、键盘焦点和已收藏四种状态，并提供可读的 `aria-label`。

### 热点排行

前 3 名可用铜、青和浅钢色区分，其余使用中性色。热度是站内多来源及时间信号，不以虚构精确分数制造权威感。

### 筛选器

搜索框和筛选控件使用可见标签或无障碍名称。已启用条件显示为可逐个移除的筛选胶囊，并提供“清除全部”。空结果提示当前条件和恢复入口。

### 详情面板

宽屏最大宽度约 560px；移动端全屏。顶部固定标题区和关闭按钮，主体依次展示：原始标题、双语摘要、关键事实、来源与语言、发布/抓取时间、关联报道、原文按钮。焦点进入面板并在关闭后返回触发元素。

### 轴承元素

- 字标中的轴承为几何线稿，不使用复杂照片缩略图。
- 首屏可使用大型轴承剖面或滚珠轨迹作为低对比背景，避开正文阅读区。
- 分割线、时间线节点和图表标记可以借用滚珠/滚道形态。
- 黄铜只用于保持架、精选和关键数据，不铺满大面积背景。

## 5. 页面特征

- **精选**：顶部状态条 + 热点榜，随后重点报道、时间线与品牌七日关注度；演示声明在首屏可见。
- **全部动态**：强化搜索与筛选工具区，信息卡密度最高。
- **品牌雷达**：品牌矩阵、关注度条和当前品牌信息流；显示“关注名单，非权威排名”。
- **市场未来**：趋势卡片配成熟度/信号强度等定性标识，不展示无来源预测数字。
- **论文研究**：更接近期刊索引，突出 DOI、作者、期刊、引用和开放获取状态。
- **收藏**：保留内容类型筛选；空状态说明收藏仅存本机浏览器。

## 6. 交互与无障碍验收

- 所有操作支持键盘，焦点环清晰且不会被容器裁剪。
- 抽屉和详情面板正确设置名称、模态语义、焦点圈定和 Escape 关闭。
- 文本与背景对比度至少 4.5:1，大字号及非文本控件至少 3:1。
- 图标按钮都有可读名称；时间、风险和来源等级不只靠颜色传达。
- 390px、768px、1440px 下无非预期横向滚动，长标题、作者列表和 DOI 可正确换行。
- 用户选择的语言和主题重新载入后保持；系统减少动态效果设置得到尊重。
